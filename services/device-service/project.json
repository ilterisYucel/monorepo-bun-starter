{
  "name": "device-service",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/device-service/src",
  "projectType": "application",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun --watch run.ts",
        "cwd": "services/device-service"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun build src/index.ts --outdir dist --target bun",
        "cwd": "services/device-service"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun run run.ts",
        "cwd": "services/device-service"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests",
        "cwd": "services/device-service"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist",
        "cwd": "services/device-service"
      }
    }
  },
  "implicitDependencies": ["core", "shared-types", "simulators"]
}
