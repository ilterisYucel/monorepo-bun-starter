// ponytail: rack offset removed — frontend now uses composite { deviceId, rackId } keys
// Deleted: extractChargeStatus, deriveStatusEntries (moved to device-service)
// Deleted: applyRackOffset (no longer needed)
