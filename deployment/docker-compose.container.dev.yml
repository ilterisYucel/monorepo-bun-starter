name: container-stack

services:
  timescaledb:
    image: timescale/timescaledb:latest-pg14
    container_name: container-timescaledb-dev
    command:
      - -cshared_buffers=256MB
      - -cwork_mem=32MB
      - -ceffective_cache_size=768MB
      - -cmaintenance_work_mem=64MB
      - -cmax_connections=50
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: battery
    ports:
      - "5433:5432"
    volumes:
      - timescale-data-dev:/var/lib/postgresql/data
    networks:
      - container-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d battery"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  redis:
    image: redis:7-alpine
    container_name: container-redis-dev
    command: >
      redis-server
      --maxmemory 64mb
      --maxmemory-policy allkeys-lru
      --save ""
      --appendonly no
    ports:
      - "6380:6379"
    volumes:
      - redis-data-dev:/data
    networks:
      - container-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  device-service:
    build:
      context: ../
      dockerfile: packages/services/device-service/deployment/Dockerfile.dev
    container_name: container-device-service-dev
    environment:
      NODE_ENV: development
      REDIS_HOST: redis
      REDIS_PORT: 6379
      DEVICE_CONFIG_DIR: /app/config-docker
    volumes:
      - ../packages/services/device-service/src:/app/packages/services/device-service/src
      - ../packages/services/device-service/run.ts:/app/packages/services/device-service/run.ts
      - ../packages/services/device-service/deployment/config-docker:/app/config-docker:ro
      - ../packages/core/src:/app/packages/core/src
      - ../packages/shared-types/src:/app/packages/shared-types/src
      - ../packages/shared-utils/src:/app/packages/shared-utils/src
      - ../packages/simulators/src:/app/packages/simulators/src
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - container-network
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  data-service:
    build:
      context: ../
      dockerfile: packages/services/data-service/deployment/Dockerfile.dev
    container_name: container-data-service-dev
    environment:
      NODE_ENV: development
      REDIS_HOST: redis
      REDIS_PORT: 6379
      TIMESCALE_HOST: timescaledb
      TIMESCALE_PORT: 5432
      TIMESCALE_USER: postgres
      TIMESCALE_PASSWORD: password
      TIMESCALE_DATABASE: battery
    volumes:
      - ../packages/services/data-service/src:/app/packages/services/data-service/src
      - ../packages/services/data-service/run.ts:/app/packages/services/data-service/run.ts
      - ../packages/core/src:/app/packages/core/src
      - ../packages/shared-types/src:/app/packages/shared-types/src
      - ../packages/shared-utils/src:/app/packages/shared-utils/src
    depends_on:
      redis:
        condition: service_healthy
      timescaledb:
        condition: service_healthy
    networks:
      - container-network
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  web-service:
    build:
      context: ../
      dockerfile: packages/services/web-service/deployment/Dockerfile.dev
    container_name: container-web-service-dev
    environment:
      NODE_ENV: development
      PORT: 5001
      HOST: 0.0.0.0
      JWT_SECRET: dev-secret-change-in-production
      REDIS_HOST: redis
      REDIS_PORT: 6379
      POSTGRES_HOST: timescaledb
      POSTGRES_PORT: 5432
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DATABASE: battery
      DEVICE_CONFIG_DIR: /app/packages/services/device-service/config
      TIMESCALE_CHUNK_INTERVAL: "6 hours"
      TIMESCALE_COMPRESS_AFTER: "1 day"
      TIMESCALE_RETENTION_AFTER: "90 days"
    ports:
      - "5001:5001"
    volumes:
      - ../packages/services/web-service/src:/app/packages/services/web-service/src
      - ../packages/services/web-service/run.ts:/app/packages/services/web-service/run.ts
      - ../packages/core/src:/app/packages/core/src
      - ../packages/shared-types/src:/app/packages/shared-types/src
      - ../packages/shared-utils/src:/app/packages/shared-utils/src
    depends_on:
      redis:
        condition: service_healthy
      timescaledb:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5001/health"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    networks:
      - container-network
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  web:
    build:
      context: ../
      dockerfile: apps/container-web/deployment/Dockerfile.dev
    container_name: container-web-dev
    environment:
      NODE_ENV: development
      VITE_WEB_SERVICE_URL: http://web-service:5001
    ports:
      - "5173:5173"
    volumes:
      - ../apps/container-web/src:/app/apps/container-web/src
      - ../apps/container-web/index.html:/app/apps/container-web/index.html
      - ../packages/shared-types/src:/app/packages/shared-types/src
      - ../packages/shared-utils/src:/app/packages/shared-utils/src
      - ../packages/ui/src:/app/packages/ui/src
      - ../tsconfig.base.json:/app/tsconfig.base.json
      - /app/apps/container-web/node_modules
      - /app/node_modules
    depends_on:
      web-service:
        condition: service_healthy
    networks:
      - container-network
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  timescale-data-dev:
  redis-data-dev:

networks:
  container-network:
    driver: bridge
